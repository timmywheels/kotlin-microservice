dependencies {
    compile project(":composition")
    compile project(":domain")
    compile project(":persistence")

    testCompile project(":composition").sourceSets.test.output
    testCompile project(':domain').sourceSets.test.output
}